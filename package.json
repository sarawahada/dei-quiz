{
  "name": "dei-quiz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node ./dei-quiz-backend/server.js",
    "build": "cd dei-quiz-frontend && npm install && npm run build",
    "install-all": "npm install && cd dei-quiz-frontend && npm install && cd ../dei-quiz-backend && npm install"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "concurrently": "^8.2.0"
  }
}
